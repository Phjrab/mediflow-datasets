# Hair 발표용 그림

PNG 300 dpi, PDF는 동일 고해상도 래스터 그림입니다.
원본 기록과 기존 이미지는 변경하지 않았습니다. source_data.json에 원값과 출처 SHA-256을 보존했습니다.
막대 축은 0에서 시작하며 F1 열지도 범위는 0~1입니다. 수치 반올림 없이 원값을 보존하고, F1 값은 열지도에 원값 그대로 표시합니다.
원본 Epoch JSON 미확보: 저장된 곡선을 재배치. Loss 정의가 달라 절대값 순위 비교 불가.
